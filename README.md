# Md Mashfiq Rizvee — Academic Portfolio

A lightweight academic portfolio built with plain HTML and CSS for GitHub Pages.

## Publish on GitHub Pages

1. Create a repository named `<your-github-username>.github.io`.
2. Copy the contents of this folder into the repository root.
3. Commit and push to the `main` branch.
4. In GitHub: **Settings → Pages → Build and deployment → Deploy from a branch → main / root**.
5. Your site will appear at `https://<your-github-username>.github.io/`.

## Files

- `index.html` — site content
- `stylesheet.css` — responsive academic styling
- `assets/mashfiq-rizvee-cv.pdf` — downloadable CV
- `assets/favicon.svg` — initials favicon

## Optional next edits

- Add a professional headshot and place it in `assets/`.
- Add project/paper thumbnails if you want a more visual publications section.
- Add direct links for papers currently listed without DOI/project pages.
- Add a custom domain by creating a `CNAME` file.
